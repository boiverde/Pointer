export enum OrderStatus {
    PENDING = 'PENDING',
    PAID = 'PAID',
    SHIPPED = 'SHIPPED',
    DELIVERED = 'DELIVERED',
    CANCELLED = 'CANCELLED',
}

export enum StockMovementType {
    SALE = 'SALE',
    RESTOCK = 'RESTOCK',
    RETURN = 'RETURN',
    ADJUSTMENT = 'ADJUSTMENT',
}
